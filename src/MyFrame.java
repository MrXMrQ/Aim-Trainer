import javax.swing.*;

public class MyFrame extends JFrame {
    public MyFrame() {
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setSize(780,480);
        setResizable(false);
        setVisible(true);
        setTitle("Aim trainer");
    }
}
